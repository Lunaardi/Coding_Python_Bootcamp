# Examples:
# Removes the size of the list, that is, it checks the number of elements or objects that the list has.			

linguagens = ["python", "js", "c", "java", "csharp"]
					
print(len(linguagens)) 
# >>> 5
