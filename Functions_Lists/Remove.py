# Example:
# It is a second way to remove elements/objects from a list.
# Unlike pop where we pass the object index, in this case we remove the object itself.

linguagens = ["python", "js", "c", "java", "csharp"]
					
linguagens.remove("c")
					
print(linguagens)
#>>> ["python", "js", "java", "csharp"]
